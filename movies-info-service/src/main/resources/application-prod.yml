spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      #uri: mongodb://localhost:27017/movies-review-service
      host: localhost
      port: 27017
      database: movies_review_db

server:
  port: 8082

